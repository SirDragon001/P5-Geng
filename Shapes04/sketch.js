
var x = 0;
var y = 0;
var framesElapsed = 0;

function setup() {
  createCanvas(windowWidth, windowHeight);
  noStroke();

  x = windowWidth / 2;
  y = windowHeight / 2;
}

function draw() {
  background(0);
  

  noFill();

  text('Value of X: ' + x,200,80);
  text('Value of XL: ' + xl,200,100);

  



}

